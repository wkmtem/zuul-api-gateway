eureka:
  client:
    service-url:
      # ubuntu
      defaultZone: ${eurekaServerUrl:http://administrator:omega@192.168.0.201:7777/eureka/}
      # 139.196.195.162
      #defaultZone: ${eurekaServerUrl:http://administrator:omega@10.46.31.101:7777/eureka/}
      # localhost
      #defaultZone: ${eurekaServerUrl:http://administrator:omega@127.0.0.1:7777/eureka/}

spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: interview-config-server
      name: zuul_gateway, switch
      profile: ubuntu
      #label: master

  rabbitmq:
    host: ${rabbitmqHost:139.196.195.162}
    port: ${rabbitmqPort:5672}
    username: ${rabbitmqUsername:admin}
    password: ${rabbitmqPassword:omega}